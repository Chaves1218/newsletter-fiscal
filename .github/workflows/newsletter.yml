# .github/workflows/newsletter.yml
name: Newsletter Fiscal Cooperflora

on:
  schedule:
    # Roda todo dia às 10h00 horário de Brasília (13:00 UTC)
    - cron: "0 13 * * 1-5"   # segunda a sexta
    # Para incluir sábado também, use: "0 13 * * 1-6"
    # Para todos os dias:             "0 13 * * *"
  workflow_dispatch:           # permite rodar manualmente pelo GitHub

jobs:
  enviar-newsletter:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 📥 Checkout do repositório
        uses: actions/checkout@v4

      - name: 🐍 Configurar Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Instalar dependências
        run: pip install -r requirements.txt

      - name: 📰 Executar newsletter
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
        run: python newsletter.py
